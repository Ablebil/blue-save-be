export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum ReportStatus {
  WAITING = "WAITING",
  VERIFIED = "VERIFIED",
  INVALID = "INVALID",
  RESOLVED = "RESOLVED",
}

export enum Category {
  ENVIRONMENT = "ENVIRONMENT",
  CONSERVATION = "CONSERVATION",
  EDUCATION = "EDUCATION",
  RESEARCH = "RESEARCH",
}
